# minimum_effect errors when x is not a bdlnm output or basis wrong

    `x` must be the list returned by `bdlnm()`: it should contain the fitted INLA model "$model" and the posterior samples matrix "$coef".

---

    The `basis` argument must be an object of class <crossbasis>.

---

    The `basis` argument must be an object of class <crossbasis>.

